.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
config.yaml

# Local Claude config (never commit)
.claude/

# Stray local artifact dirs (browser-test output, etc.)
output/

# Intranet / real-data artifacts — NEVER commit.
# These contain real internal Confluence URLs, staff names, and JIRA links.
inputs/
*3725660167*
config.kb.page-*.yaml
# All pipeline outputs are fully regenerated by `python -m backend.pipeline
# demo` (clean_demo_outputs wipes outputs/, then every step rebuilds it). The
# seed/inputs live in fixtures/ and config — NOT here — and the test suite
# regenerates outputs/ in setUpClass, so nothing under outputs/ needs tracking.
# Tracking it only created churn that buried real code diffs (the worst being
# the ~88k-line store.json + loaded_descriptions.json regenerated every run,
# plus a fresh chroma uuid dir per run).
outputs/
