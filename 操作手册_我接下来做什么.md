# 操作手册 · 我(Ethan)接下来做什么

> 给你一个人看的"导航 + 行动清单"。东西多,但你的角色其实只有一个:**协调三方(codex / 同事 / opencode)+ 拍几个关键决定 + 最后给领导演示**。代码不用你写。

---

## 0. 一句话现状

我们在做"Confluence 知识问答 PoC",要用证据对比**三种回答方式**(RAG / LLM+Wiki卡片 / Agentic),最后给领导看哪条路线最好。

**进度条:**

```
[✅ 离线代码跑通] [✅ 三族+演示看板] [✅ module多标签] [🔶 同事词表(评审中)]
        → [⬜ 离线收尾] → [⬜ 进内网接4个适配器] → [⬜ 真数据跑eval] → [⬜ Business评审] → [⬜ 领导演示]
```

现在卡在两件事上(下面"今天就做"):**①同事词表有没有别名列;②让 codex 把离线最后两项收尾**。这两件不解决,后面进内网会返工。

---

## 1. 谁干什么(你只是中间的协调人)

| 角色 | 干什么 | 你怎么对接 |
|---|---|---|
| **codex**(外网编码) | 写所有不依赖内网的代码 | 给它 `handoff/01、04、05、06` |
| **同事**(已做完词表) | 出 key topics + keyword list + module,Business 审 | 你去对齐"词表格式 + 别名列" |
| **opencode**(内网编码) | 只接 4 个适配器(MCP/gpt-5.5/embedding/向量库)+ 配置 | 给它 `handoff/02、03` |
| **Business** | 审卡片/倒排表(精度/召回) | 给它 `card_ingestion_sop/templates/business_review_checklist` |
| **你(Ethan)** | 协调 + 拍决定 + 跑/盯流程 + 给领导演示 | 看本手册 |

---

## 2. 接下来的步骤(按顺序,每步含"为什么")

### 阶段 A · 让 codex 把离线代码收尾(外网)
- **为什么**:进内网后你只想"换 4 个适配器",不想在内网里改业务逻辑(内网难调试、难复现)。凡是不依赖内网的,先在外网写完。
- **你做什么**:给 codex 两条明确任务,确认是否已完成:
  1. **CR-2(最关键)**:把 skill 的**真实 prompt** 接进 `map/reduce/answer/intent` 的 system 字符串。现在那里是占位标签 + MockLLM 启发式,**换成 gpt-5.5 后会产出垃圾**。依据 `handoff/04_CODE_REVIEW.md` CR-2。
  2. **golden 生成**:实现 `eval/golden_gen.py`,按 `card_ingestion_sop/skills/golden-set/SKILL.md`。现在 eval 只读 15 条种子题,没有"真生成 golden"的能力。
  - (可选)真 RAGAS、过度关联(CR-4)、真 BGE-m3 的 `local` embedder。
- **完成标准**:`uv run python -m backend.pipeline demo` 仍全绿;map/reduce 的 system 是完整 prompt;golden_gen 能跑出题。

### 阶段 B · 和同事对齐词表(合并的关键)
- **为什么**:我们的 reduce 是靠"原文里的叫法 → 匹配规范名 + **别名**"来把内容归到主题、建卡片。她的表如果**没有别名(aliases)列**,`DMP / Data Management` 就归不进 `DM Plugin`,卡片会大面积建不起来。这是两边合并的命根子。
- **你做什么**:问同事一句——"**你的主题表里有没有别名/同义词那一列?**"
  - 有 → 让她按 `handoff/05_合并对接_module_facet.md` §1.3 的 JSONL 格式导出**审批过**的表 → 放到 `inputs/keyword_table.jsonl`。
  - 没有 → 三选一(她加一列 / 我们加一步别名解析 / 用她的 keyword list 当别名),见 `05` §1.2。
- **完成标准**:`inputs/keyword_table.jsonl` 就位,`load_vocabulary` 能读。

### 阶段 C · 进内网,opencode 接 4 个适配器(内网)
- **为什么**:整套系统只有 4 处依赖内网,其余外网已就绪。
- **你做什么**:把仓库带进内网,把 `handoff/02_OPENCODE_内网补充.md` + `03_OPENCODE_RAGAS_CHROMA.md` 给 opencode,让它:实现 MCP / gpt-5.5 / embedding / 向量库 4 个 adapter;填 `config.yaml`(密钥走环境变量);确认 embedding 模型与维度。
- **完成标准**:`providers` 切到真实实现;`ingest` 能抓下真实 `2026 Planned Project` 切片。

### 阶段 D · 真数据跑全链(内网)
- **为什么**:产出**真卡片 + 真三族 eval**——这才是 PoC 的真正交付物。
- **你做什么**(或盯 opencode 做):按顺序 `ingest → map → reduce(用同事词表)→ load → golden_gen(生成真 golden)→ eval`。先在 5–10 页上 eyeball 切块和卡片质量,别一上来全量。
- **完成标准**:`outputs/eval_report.json` 用的是真 embedding + gpt-5.5 judge(前端看板**不再显示 DEMO 横幅**)。

### 阶段 E · Business 评审闭环
- **为什么**:保证质量,并启动"自进化 SOP";她指出的"漏页"正好是 `association_recall` 的真值。
- **你做什么**:用 `card_ingestion_sop/templates/business_review_checklist_评审清单.md`,把**倒排表 + review_queue + 卡片抽样**按切片批量给 Business;反馈 → 登记 bad case → 提炼规则回写 skill → 重跑。
- **完成标准**:一轮评审闭环跑通,词表/卡片按反馈更新。

### 阶段 F · 过闸门 + 给领导演示
- **为什么**:PoC 的真交付 = 用证据说明哪条路线最好,而不是"我觉得"。
- **你做什么**:看冻结 golden 上**卡片/agentic 有没有跑赢纯 RAG**(重点看 `association_recall` 和 `漏钻率`)。用前端"评估看板"的**三族对比**给领导演示——**真数据版**(无 DEMO 横幅)。
- **完成标准**:有数据支撑的方案选型结论;领导看过、拍板下一步。

### 阶段 G · 扩切片
- 选下一个切片,重复 B–F。每转一圈 skill 更准、覆盖更全。

---

## 3. 你现在就要拍的 3 个决定

1. **别名列**(问同事)—— 阻塞阶段 B/D。今天就问。
2. **让 codex 先收尾 CR-2 + golden 生成再进内网**(强烈建议是)—— 不做,真数据上 gpt-5.5 产出和评测都不可靠。
3. **内网 embedding 用哪个模型 + 维度**(问内网平台:能不能直接用 BGE-m3 1024 维?不能就给替代模型和维度)—— 影响阶段 C 建表。

---

## 4. 文件地图(我该看哪个)

| 我想… | 看这个 |
|---|---|
| 懂整体方案/三条路线 | `Confluence_QA_PoC_方案讨论稿.md` |
| 懂卡片 SOP/流程闭环 | `card_ingestion_sop/README_数据摄取卡片SOP.md` |
| 给 codex 派活 | `handoff/01_CODEX_构建规格.md`、`04_CODE_REVIEW.md`、`05_合并对接_module_facet.md`、`06_FRONTEND_REVIEW.md` |
| 给 opencode 派活(内网) | `handoff/02_OPENCODE_内网补充.md`、`03_OPENCODE_RAGAS_CHROMA.md` |
| 给 Business 评审 | `card_ingestion_sop/templates/business_review_checklist_评审清单.md` |
| golden 怎么生成 | `card_ingestion_sop/skills/golden-set/SKILL.md` |
| 怎么跑代码/命令 | 仓库 `README.md`,或 `handoff/02` §2 启动方式 |

---

## 5. 今天就做(TL;DR)

1. 问同事:**"主题表里有没有别名/同义词列?"**(决定合并怎么接)
2. 给 codex 派:**CR-2(接真 prompt)+ 实现 golden_gen**(`handoff/04` + `golden-set` skill)
3. 问内网平台:**embedding 用哪个模型、多少维**

这三件一动,后面就是"进内网 → 跑真数据 → 评审 → 给领导"一条线下去了。
