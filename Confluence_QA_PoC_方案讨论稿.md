# Confluence 知识问答 PoC —— 技术方案讨论稿

> **文档目的**：梳理三条技术路线（纯 RAG / 纯 LLM 总结卡片 / 混合），供团队评审。
> **状态**：讨论稿，待团队评审。评审通过后再收敛成给 opencode 的决策/实现 spec。
> **范围**：先做一个纵切片（如 `06-Delivery/10. Planned Project/2026 Planned Project`），不做全量。
> **说明**：本稿综合自方案讨论及并行的 RAG PoC 设计；重点是把"用 RAG 检索"与"把 Confluence 总结成卡片让 LLM 回答"两条路讲清、讲全，并给出推荐的组合方式。

---

## 1. 背景与约束

- **数据源**：本部门 Confluence。文档数量多、page tree 深但结构整齐（顶层分区如 `01-introduction / 02-Engagement / 04-Design / 05-Test Strategy / 06-Delivery / 07-Quality Assurance / 08-Way of Working / …`）。
- **已有能力**：
  - 内网 **gpt-5.5** 可调用（负责生成/总结/裁判，不负责向量检索质量）。
  - **MCP** 可获取 Confluence 的结构与正文。单页返回字段：`title, source_type, confluence_space, confluence_page_id, source_url, owner, labels, captured_at, update_at, confluence_version` + 正文（Markdown，含表格；图片存本地路径）。ancestor / tree path 在标题与正文之间，ingestion 时解析进 metadata。
  - 向量库用 **pgvector**（向量 + 全文检索 + 过滤三合一在一个库里）。
- **目标**：让 LLM 基于 Confluence 内容回答用户问题，并能**引出原文**。
- **PoC 的唯一真正交付物**：一套能客观对比不同方案效果的**评测**。没有评测就无法判断哪条路线更好。

---

## 2. 术语与核心概念（先统一语言，避免歧义）

| 术语 | 含义 |
|---|---|
| **reference（原文 chunk）** | 切块后的原始 Confluence 内容片段，是最终喂给 LLM、用于回答的"料"。 |
| **description / 索引层** | 用于检索命中的文本，可以是正文嵌入，也可以是为 chunk 生成的"假设问题"（hypothetical question）。命中后取回真实 reference。 |
| **卡片（card）** | **一条总结记录**（一个 md / 一行 DB 记录）。是"选择层"挑选的最小单位，承载模板字段 + 回原文的锚点。即下文"总结卡片 / wiki 文档"。 |
| **关键词 / topic** | 业务定义的**受控词表**（如 DM plugin、journey plugin），是卡片的路由键与合并键。 |
| **模板 / framework** | 卡片的字段 schema（业务提供），规定每张卡片要收集哪些信息。 |
| **锚点（anchor）** | 卡片里每条信息指回原文确切位置的指针（`page_id` + heading 路径 / URL 锚点）。 |
| **map / reduce** | map = 逐页抽取；reduce = 跨页合并成卡片。 |
| **选择层 / 路由** | 决定"该看哪张卡片 / 哪段原文"的环节（LLM 沿树走 或 向量检索）。 |
| **grounding（回原文取证）** | 回答前取回原文，依据原文作答并引用，而非依据总结作答。 |

---

## 3. 三条技术路线总览

| 维度 | 方案一：纯 RAG | 方案二：纯 LLM（总结卡片） | 方案三：混合（推荐） |
|---|---|---|---|
| 精确细节（配置/错误码/表格） | **强**（直取原文 chunk） | 弱（总结易丢细节） | **强**（路由后回原文取证） |
| 概念/关系/"全貌"类问题 | 弱（chunk 碎片化） | **强**（跨页综合卡片） | **强** |
| 覆盖范围 | **全**（所有原文可检索） | 受限（只覆盖业务圈定的 topic） | **全**（卡片 + RAG 兜底） |
| 规模 / token | 省（按需取 chunk） | 重（整篇卡片入 context；选择随规模膨胀） | 可控（分层路由 + chunk 取证） |
| 构建成本 | 中（切块 + 嵌入） | 高（业务框架 + 全量 map-reduce + 人审） | 高（两者叠加） |
| 维护 / 同步 | 低（页变只重嵌该页） | 高（跨页卡片需重新 reduce） | 中高 |
| 可解释 / 引用 | 强 | 弱（除非卡片带锚点） | 强 |
| 人可读知识库副产品 | 无 | **有**（wiki 卡片） | **有** |

**一句话定位**：
- 方案一靠**向量相似度**选内容，方案二靠**LLM/业务规则**选内容——它们是"选择层"的两种实现，底层都需要"引出原文"。
- 方案二的"卡片"其实就是方案一里"description / summary 层"的另一种形态。二者不是对立，可叠加。
- **推荐方案三**：卡片负责"找对地方"（路由 + 答概念/关系题），原文负责"给对答案"（grounding），RAG 兜住卡片覆盖不到的长尾。**卡片是顶，原文是底。**

---

## 4. 方案一：纯 RAG（embedding 检索）

### 4.1 三层结构
- **spec（类型标注）** = metadata，检索前按类型/平台预过滤。
- **description（描述/假设问题）** = 索引层，命中后取回真实 chunk。
- **reference（真实 chunk）** = 最终作答依据。

### 4.2 ingestion（切块）
- MCP 返回整页 Markdown，切块在我方做。
- **结构感知切块**：按 heading（`##` H2 / `###` H3）解析成树，最深一层 heading + 其正文作为一个候选 reference；完整 heading 路径写进 metadata。
- 规则：表格 / 代码块 / 列表**绝不从中间切断**；过小 section 向上并入父级；过大 section 在段落边界二次切分 + 少量 overlap；无 heading 的页退化为按段落/长度切。
- 默认参数：chunk 目标 200–800 token、上限 ~1000、<100 向上合并、overlap ~50。

### 4.3 检索
- **混合检索**：pgvector 余弦（向量）+ Postgres `tsvector`（全文/BM25 类）两路，用 **RRF（倒数排名融合）** 合并。技术文档含大量精确标识符（接口名、错误码、ticket ID），纯向量必漏，混合是刚需。
- **metadata 过滤**：普通 SQL `WHERE`（facet 做成列或 jsonb）。
- **rerank**：加一个 reranker 重排，是性价比最高的质量杠杆，建议开。
- 默认 `top-k=8`。

### 4.4 多向量映射（pgvector 落库）
- `references` 表：一行一个 chunk，带 embedding 与 metadata。
- `descriptions` 表：一行一条"假设问题"，各自带 embedding，外键指向 reference；检索打问题表 → join 回 reference → **去重**（多条问题命中同一 chunk 只返回一次）。
- `summary` 节点：页面/主题级摘要，供"全貌/综合"类问题先命中、再指向多个 reference。

### 4.5 优缺点
- **优点**：精确细节强、覆盖全、构建相对机械、引用清晰、维护成本低。
- **缺点**：chunk 碎片化，对"X 和 Y 怎么配合""某模块全貌"这类跨页综合问题弱；纯靠它缺少业务视角的知识组织。

---

## 5. 方案二：纯 LLM（总结卡片 / skills）

### 5.1 思路
把 Confluence 内容**跨页**总结成一批"common knowledge 卡片"，每张卡片对应一个业务关键词/topic，按受控模板填充。提问时由一个答题 agent 选中相关卡片来回答。

agent 回答时有两个**动作**可选：
- **(a) 直接拿卡片作答**：读卡片总结 → 据总结作答，原文最多作链接。最快，但**答案被总结保真度卡死**，精确细节（如"retry 默认 3 次"）容易在总结时就丢。
- **(b) 回原文取证**：顺卡片锚点取回原文 → 据原文作答并引用。多维护"原文 + 锚点"，但保住细节。

**纯方案二 = 永远只做 (a)**（因此精确题易丢细节）。把"何时该做 (b)"交给 agent 临场判断，就升级成**方案三（§6，推荐）**。

### 5.2 数据处理流水线：跨页 map-reduce
业务人员提供 **framework（模板）+ 关键词**，并给出 70–80% 相关联的页（其余可能遗漏）；LLM 对照模板补全。具体两阶段：

**Stage 1 — Map（逐页，覆盖切片内全部页）**
- **必须全量跑**：对切片内**每一页**做抽取，而不仅是业务点名的 70–80%。这是 keyword→page 关联能补回遗漏那 20–30% 的**地基**；若只跑业务给的页，遗漏页从未被索引，就无从捞回。业务那张页表只当**合并时的种子/权重**，不当输入过滤器。
- 把**受控关键词表**喂给 LLM，让每页**自报命中哪些关键词**（输出"本页涉及：[DM plugin, retry, …]"）+ 按模板抽取相关字段。这样合并就退化成"按关键词 group by"，且业务没点到、但内容相关的页也能靠自报标签被捞进来。
- 每条抽取都**记录原文锚点**（`page_id` + heading）。
- **产物一物三用**：per-page 抽取 = ① 关联用的 keyword→page 倒排索引；② 跨页卡片的原料；③ 同时就是方案一里的 description / summary 节点。一遍 map，三处复用。

**Stage 2 — Reduce（跨页合并成卡片）**
- 按关键词 group by，把多页的同一 topic 信息合并成一张卡片。
- **narrative 字段**（定义、功能）可由总结合并；**精确字段**（配置项/数值/表格/错误码）**顺锚点回原文原样取**，不要"总结的总结"——否则经历两次有损压缩（原文→页总结→合并卡片），精确值极易丢失。
- **冲突处理**：不同页可能不一致（旧页 retry=3、迁移页改成 5）。按 `confluence_version` / `update_at` **优先取新**，并**显式标注冲突**，不让 LLM 悄悄挑一个。

### 5.3 keyword→page 倒排表的两个副产品（几乎白捡）
- **当业务审核界面**：不必让业务重读文档，只给他们看"每个关键词关联到了哪些页"，他们一眼能挑出关联错的（精度）与该在却没在的页（召回，补那 20–30%）。把"靠人补全"从"重读全部"降级成"审一张表"。
- **反向暴露漏列的 topic**：在多页反复出现、却不在业务关键词表里的词/簇，就是"大家都在讲、但没被列成 common knowledge"的候选新卡片。

### 5.4 卡片单元：按 page 还是按 entity？
- 现实里一页常提多个 entity，一个 entity 又散在多页。
- 因业务已用**受控关键词表**人工圈定 topic，**规避了自动实体抽取/归并这个最难的环节**，所以这里采用**按业务 topic/关键词出卡片**（一种受控的 entity 卡片），由 Stage 2 跨页聚合。
- 跨页关系先用卡片里的"相关组件"字段**软链接**，不在 PoC 阶段做全量实体归并。

### 5.5 增量更新
- 页 `confluence_version` 变 → 只重跑该页的 map → 再触发它参与的卡片 reduce，避免每次全量重算。
- map 是 O(N) 的 LLM 调用：切片阶段忽略不计；全量上线时务必做成增量。

### 5.6 总结模板（卡片 schema）
- **原则**：字段由"用户会问什么"倒推，**必须包含精确事实字段**，不能只有"定义/功能"。
- **示例骨架（plugin / 组件类）**：

  | 字段 | 说明 | 来源策略 |
  |---|---|---|
  | 定义 | 这是什么 | narrative，可总结 |
  | 功能 / 职责 | 干嘛的 | narrative，可总结 |
  | 配置项 | 逐项含默认值、取值范围 | **精确，回原文原样取** |
  | 接口 / 依赖 | 依赖谁、被谁依赖 | 半精确，带锚点 |
  | 与其他组件的关系 | 喂给"how plugins work together"类综合问题 | narrative + 软链接 |
  | 常见问题 / 坑 | troubleshoot | 带锚点 |
  | 原文锚点 | 每节指回原文 | 必填 |

- **要点**：narrative 字段总结得起；精确字段（配置、错误码、整张表格、代码）**要么原样搬、要么干脆留给原文**——再好的模板也塞不下一张 30 行配置表。
- 可按 `content_type`（流程 / 设计 / runbook / FAQ…）设**多套模板** + 一个 catch-all 字段兜住模板没覆盖的要点。

### 5.7 优缺点
- **优点**：概念/关系/"全貌"类问题强；产出人可读、可维护的 wiki；业务视角的知识组织。
- **缺点**：(a) 答案受总结保真度限制、精确细节易丢；覆盖只到业务圈定的 topic；选择层在卡片很多时易选错、token 易膨胀；需要业务投入 + 持续维护与同步。

---

## 6. 方案三：Agentic 卡片优先、按需下钻（推荐）

> 这是把方案一、二组合起来的**推荐方案**。核心不是"事先规定哪类问题走哪条路"，而是让一个 **agent** 临场决定：默认用卡片答，判断卡片不足以可靠回答时，再调"取原文"工具下钻。

### 6.1 心智模型
把 agent 想成一个资深同事，手里有两件工具——**卡片**（脑子里的总结）和**取原文**（去翻文档）：
- 你问概念，他张口就答（用卡片）。
- 你问具体数值，他会说"我翻下文档"，给你确切值 + 出处（下钻取原文）。

所以 §5.1 的 (a)/(b) 不是两个独立方案，而是这个 agent 每次在两个**动作**里二选一。纯方案二 = 永远只做 (a)；方案三 = 给它加上"下钻"这个动作，以及"何时该用"的判断。

### 6.2 前提：卡片内容分三档，并"对自己诚实"
agent 能不能直接答，取决于卡片里那条信息是哪一档。建卡片时（§5.6 模板）就按三档存：

| 档位 | 例子 | agent 能否直接答 |
|---|---|---|
| **narrative**（定义/功能/关系） | "DM plugin 负责把消息投递到各渠道…" | **能**，概念类直接答 |
| **精确-内联**（值带锚点原样搬） | `max_retry: 3`　来源 `PageB#Configuration` | **能**，且自带出处（≈ 已取证） |
| **仅指针**（卡片不放值，只给位置） | "重试相关配置见 `PageB#Configuration`" | **不能**，必须下钻取原文 |

**关键纪律**：卡片对自己诚实——有确切值就连出处一起放，没有就只给指针，**绝不在卡片里编一个像样的值**。这样 agent 的判断就从"我这总结够不够好"（主观、不可靠）变成"卡片这个字段是有值、还是只有指针"（客观、可靠）。

### 6.3 下钻决策流程（可直接落地）

```
收到用户问题：
1) 找卡片（沿 page tree/关键词，或向量检索卡片）→ 命中卡片？
     否 → 走方案一 RAG over 原文，据原文答 + 引用。            [兜底]
     是 ↓
2) 判断问题意图：
   ├ 概念 / 全貌 / 关系类
   │    → 用卡片 narrative 直接答 + 标注卡片来源。              [不下钻]
   └ 精确类（具体值 / 配置 / 错误码 / 版本相关行为 / 精确步骤）
        → 看卡片对应字段：
            "内联值 + 锚点"  → 据值答 + 引用（高风险值仍可回源核，见 6.5）
            "仅指针" 或缺失  → 顺锚点取回原文 section → 据原文答 + 引用。  [下钻]
3) 任何时候：用户追问更细 / 表示怀疑 → 强制下钻取原文。        [追问是补充触发，非唯一触发]

硬规则：事实类 claim 必须带可点击引用；给不出带锚点的出处，就必须下钻。
```

⚠️ **最关键的一条：不要只靠"用户追问"来触发下钻。** 精确问题上，用户分不清"自信且对"和"自信但错"——卡片若只含"支持 retry 配置"，agent 答"可以配 retry"听着没毛病，用户不会追问，错/缺的答案就这么过去了。所以要靠"问题意图 + 字段诚实度"**主动**下钻（第 2 步），把"追问"只当额外补的一道（第 3 步）。

### 6.4 走查五个例子
1. **概念题**："DM plugin 是干嘛的？" → 意图=概念 → 读卡片 narrative → 直接答 + 卡片来源。不下钻。
2. **精确题·卡片有内联值**："DM plugin retry 默认几次？" → 意图=精确 → 卡片配置字段是 `max_retry: 3` [PageB#Config] → 答"3 次"并引用。（其实已等于取过证）
3. **精确题·卡片只有指针**："DM plugin 超时怎么配？" → 意图=精确 → 该字段只有指针 → 下钻取 `PageB#Config` 原文 → 据原文答 + 引用。
4. **追问下钻**：先问"DM 和 journey 怎么配合"（概念，卡片答完）→ 用户追"那具体传什么参数" → 触发下钻取原文。
5. **兜底**：问到的 topic 压根没有卡片 → 直接走方案一 RAG over 原文。

### 6.5 两个工程现实
- **新鲜度**：卡片里"内联的精确值"本质是缓存，源页 `version` 一变可能过期。高风险精确值即便卡片有，也建议回源核一遍（接 §5.5 增量更新：源页变 → 重跑该页 map → 卡片字段刷新）。
- **取原文范围**：下钻时只取命中的 section，别把整页灌进 context（控 token）。

### 6.6 为什么推荐它
- 卡片丢细节的风险被"主动下钻 + grounding"化解：概念题快（卡片）、精确题准（原文），且都带引用。
- 选择层在规模大时失准、token 膨胀的风险，被"分层路由 + 只取命中 section"压住。
- 业务只圈 70–80% 的覆盖缺口，被 RAG 兜底 + 倒排表人工审 + topic 发现共同收窄。
- 一份 map 产物三处复用（§5.2），不重复造数据。

**选择层（找哪张卡片）仍是可切换的一层**，PoC 两种都测、用评测定：agentic 沿 page tree/关键词走（适合"该看哪块 wiki"、跨页综合、多跳）；或对卡片/description 做向量检索（树路径模糊时兜底）。

---

## 7. 检索与选择层 · pgvector 实现要点

- 混合检索 = pgvector 余弦 + Postgres `tsvector`，两路 **RRF** 合并，无需引第二引擎。
- metadata 过滤 = SQL `WHERE`（facet 做成列或 jsonb）。
- 多向量：`references` / `descriptions(问题)` / `summary 节点` 三类，问题表命中后 join 回 reference 去重。
- PoC 数据量小，**先不建 HNSW、用精确检索**（recall 满，少一个干扰变量），扩量再加 ANN。
- pgvector 带索引列历史上限 **2000 维**，超了用 `halfvec`；选 embedding 时把维度算进去。
- **embedding 可插拔**：opencode 写一个 `embed()` 接口，模型从 config 注入，现在不阻塞。
- **多语种默认**：只要用户会用中文提问而文档是英文，就上多语种 embedding（如 BGE-m3 / multilingual-e5 这类可私有部署的）做跨语种检索；生成的"问题/卡片"也需含中英双语，否则跨语种召回吃亏。
- **grounding 强制**：无论哪个选择层，回答前都取回原文 chunk，带 Confluence 引用。

---

## 8. 评测方案（PoC 真正的交付物）

### 8.1 golden set
- 由 LLM 生成，但**必须与索引问题解耦**：换一套 prompt、模仿真实用户口吻（口语、关键词式、含糊），不要工整的 "what is X"。
  - ⚠️ 别用"生成索引问题"的同一套 prompt、同一批 chunk 去出评测题，否则评测题长得像索引里的问题，recall 被严重高估。
- **故意掺难题**：多跳（"how plugins work together"，gold = 多个 section）、精确标识符查找、以及该返回"查无此项"的越界问题（测精度与拒答）。
- 规模 30–50 条，每条标注 gold page/section + 参考答案；**评测集冻结**，所有变体跑同一套。
- **人工抽检 ~20%**：确认问题与 gold 对得上、答得了（LLM 出的 gold 会有错，不抽检指标带噪）。

### 8.2 指标
- `recall@k`、`faithfulness`、`context precision / recall`。
- **新增 `association recall`**：该进某卡片的页（gold），map 的 keyword→page 索引实际关联上了多少——直接量化"补 20–30%"是否有效。
- **新增 `漏钻率`**：精确类问题里，agent 本该回原文取证却直接用卡片作答的比例——方案三的头号风险指标。

### 8.3 对比矩阵（锁这几维，别铺太大）
- 答案来源 ∈ { 卡片直答 (a) / 卡片路由+回原文 (b) / agentic 自动决定（方案三）/ 纯 RAG }
- index ∈ { 正文 / 假设问题 / 卡片 }
- search ∈ { 纯向量 / 混合 }
- rerank ∈ { 开 / 关 }
- 其余固定，跑同一冻结 golden set，apples-to-apples。

### 8.4 工具
- **RAGAS** 最对口（faithfulness / answer relevancy / context precision & recall）。
- 变体批量 A/B 用 **promptfoo** 或 **DeepEval**。
- judge 用内网 gpt-5.5，但先拿几条人工标注**校准裁判**，避免 judge 自身有偏。全程可离线。

---

## 9. 已定决策与待确认项

**已定（讨论中已对齐）**
- map **全量**跑切片内所有页（关联与补漏的地基）。
- 卡片走**业务受控关键词/topic**（规避自动实体归并）。
- 精确字段 **grounding 回原文**；narrative 才用总结。
- 跨页冲突按 `version` **取新 + 标注**。
- 向量库 **pgvector**；检索**向量 + BM25 混合 + rerank**。
- **多语种默认**。
- 评测**解耦**、冻结、人工抽检、加 `association recall`。
- 推荐**混合**架构（卡片顶、原文底）。

**待确认**
- 用户**查询语言**（中文 / 英文 / 混合）—— 决定 embedding 选型与"问题/卡片"生成语言。
- **embedding 模型**具体选型（内网可用、是否多语种、能否私有部署、维度）。
- `content_type` **模板套数**（一套通用 vs 多套按类型）。
- **选择层最终形态**（agentic 走树 vs 向量检索卡片）——交由评测定。
- 卡片**粒度的演进**：先按 page-topic，是否、何时升级到跨页聚合的 entity 卡片——看评测中关系/聚合类问题表现。

---

## 10. 风险与缓解

| 风险 | 缓解 |
|---|---|
| 总结丢精确细节 | 精确字段 grounding 回原文；模板含精确字段；不做"总结的总结"。 |
| 卡片多时选择层选错 | 分层沿 page tree / 关键词路由，不平铺全部描述；向量检索兜底。 |
| token 膨胀 | 分层只展开当前分支；最终落到 chunk 级取原文，不整篇硬塞。 |
| 覆盖缺口（业务只圈 70–80% / 漏列 topic） | 全量 map + RAG over 原文兜底 + 倒排表人工审 + 反向 topic 发现。 |
| 跨页冲突 / 过期 | 按 `version` 取新、显式标注冲突；卡片绑定源页集，version 变即重算。 |
| map O(N) 成本 | 切片阶段可忽略；上线做增量（页变只重跑该页 + 受影响卡片）。 |
| 评测 recall 虚高 | 索引问题与评测问题解耦、掺难题、冻结、人工抽检。 |

---

*本稿为讨论稿，待评审后再收敛为给 opencode 的决策/实现 spec。*
